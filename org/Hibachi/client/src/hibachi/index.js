/*'use strict';

export = angular.module('app', []);

function printMessage (status) {      // default params
  let message = 'ES6';                                  // let
  console.log(`${message} is ${status}`);       // template string
}
printMessage('working');*/