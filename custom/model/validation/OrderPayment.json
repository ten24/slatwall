{
	
	"properties": {
		"order.isLockedInProcessingFlag": [{ "contexts": "processTransaction,updateAmount", "eq": false }],
		"order.isLockedInProcessingTwoFlag": [{ "contexts": "createTransaction", "eq": false }]
	}
}
